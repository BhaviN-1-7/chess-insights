"use client";

import Dashboard from "./Dashboard";

/**
 * Index page serves as the primary entry point for the Chess Insight application.
 * It currently renders the Dashboard component which contains the main analytics overview.
 */
const Index = () => {
  return <Dashboard />;
};

export default Index;